// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keine experimentellen Optionen – damit wird auf den Standard-Bundler zurückgegriffen
};

module.exports = nextConfig;
